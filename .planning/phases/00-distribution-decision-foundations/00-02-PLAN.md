---
phase: 00-distribution-decision-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/00-distribution-decision-foundations/ADR-0001-distribution-model.md
autonomous: true
requirements:
  - INST-07
  - INST-08

must_haves:
  truths:
    - "An accepted ADR document exists at .planning/phases/00-distribution-decision-foundations/ADR-0001-distribution-model.md describing the hybrid npm CLI + Claude Code Marketplace plugin distribution model"
    - "The ADR uses MADR 4.0 minimal structure (Status, Context, Decision Drivers, Considered Options, Decision Outcome, Consequences, Confirmation, Open Questions)"
    - "The ADR explicitly lists at least 4 unresolved sub-questions (marketplace orchestration, settings.json key, plugin update propagation, project-local hook stacking) for the Phase 2 spike"
    - "The ADR records hook-scope decision (project-local only, never global), uninstall total-within-scope guarantee, and zero-postinstall constraint"
  artifacts:
    - path: ".planning/phases/00-distribution-decision-foundations/ADR-0001-distribution-model.md"
      provides: "Accepted distribution-model ADR"
      contains: "Status: Accepted"
      min_lines: 80
  key_links: []
---

<objective>
Write the first deliverable of Phase 0: the accepted ADR for claude-sop's distribution model. Document the hybrid npm CLI + Claude Code Marketplace plugin decision, list rejected alternatives with rationale, and capture the unresolved questions that Phase 2's installer spike must answer.

Purpose: Lock the distribution shape in writing so all downstream phases (especially Phase 2 installer and Phase 6 license/SEA binary) build on a single, agreed model. The ADR is also the first artifact ROADMAP.md Phase 0 success criterion #1 requires.

Output: A single MADR 4.0 markdown file ready to be referenced by future plans.
</objective>

<execution_context>
@/Users/ugurgokdere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ugurgokdere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-distribution-decision-foundations/00-CONTEXT.md
@.planning/phases/00-distribution-decision-foundations/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-0001-distribution-model.md using MADR 4.0 minimal template</name>
  <files>.planning/phases/00-distribution-decision-foundations/ADR-0001-distribution-model.md</files>
  <action>
    Create the ADR using the MADR 4.0 minimal template structure described in RESEARCH.md §1. Use the draft skeleton in §1 as a starting point, but expand the following sections with content drawn from CONTEXT.md decisions A and E and RESEARCH.md §2:

    Required sections (in order):
    1. **Title:** `# ADR-0001: Distribution Model — Hybrid npm CLI + Claude Code Marketplace Plugin`
    2. **Metadata block:** Status: Accepted, Date: 2026-04-13, Deciders: project owner + claude-sop planning agent
    3. **Context and Problem Statement:** Why distribution choice matters; the two viable channels (npm registry, Claude Code Marketplace); install-time code requirement (hooks, license prompt in Phase 6); fact that marketplace plugins are passive
    4. **Decision Drivers:** ≥6 bullets — npm UX expectation, marketplace UX expectation, install-time code (hooks + Phase 6 license prompt), single source of truth, INST-07 zero-postinstall, hook scope project-local only
    5. **Considered Options:** Enumerate ALL FOUR options from RESEARCH.md §1:
       - Option 1: npm-only CLI
       - Option 2: Marketplace plugin only
       - Option 3: Hybrid where one channel points to the other
       - Option 4: Hybrid where the npm package ships the plugin bundle and CLI orchestrates registration
    6. **Decision Outcome:** Chosen Option 4. List the 5-step install flow (write local marketplace dir, register with Claude Code, install plugin, write project-scoped hooks, create config + secrets + project.json anchor) AND explicitly mention RESEARCH.md §2's recommendation: prefer Path 1 (publish a tiny GitHub-hosted marketplace.json with `{ "source": "npm", "package": "claude-sop" }`) as the initial implementation strategy. Do not commit to the orchestration code path — defer to Phase 2 spike.
    7. **Consequences:** Both Good and Bad bullets per CONTEXT.md A and RESEARCH.md §1. Must include:
       - Good: single source (npm), both segments addressed, install-time code possible, marketplace inheritance, zero divergence
       - Bad: more moving parts, uninstall must clean both paths, requires Phase 2 spike to verify orchestration mechanics
    8. **Confirmation:** Phase 2 acceptance test description — fresh machine `npx claude-sop install`, plugin appears in Claude Code's `/plugin` list AND `<project>/.claude/settings.json` has hook config; `npx claude-sop uninstall` removes BOTH.
    9. **Open Questions (tracked as issues):** At least 5 questions. Use RESEARCH.md §2's list:
       - Q1: Can `claude /plugin marketplace add <path>` run non-interactively from a child process?
       - Q2: Is there a settings.json key (e.g. `extraKnownMarketplaces`) for pre-registering marketplaces?
       - Q3: With `npm` plugin source type, does Claude Code auto-update on `npm publish`?
       - Q4: How do `<project>/.claude/settings.json` hooks interact with plugin-provided `hooks/hooks.json`?
       - Q5: Can plugin hooks be scoped per-project, or are plugin hooks always global-to-the-plugin?
       - Plus: What is the `${CLAUDE_PLUGIN_ROOT}` wipe semantic on plugin update? (from STATE.md open todos)
    10. **Cross-Phase Notes:** Brief paragraph explicitly noting:
       - INST-07: zero `postinstall` script; ALL state-changing work happens inside `npx claude-sop install`
       - INST-08: macOS + Linux only, Windows refused
       - Phase 6 will compile to a Node SEA binary; the ADR's distribution shape must remain valid for a binary artifact (not just raw JS)
       - License API key prompt happens during `install` (Phase 2), stored encrypted in `~/.claude-sop/secrets.enc` per CONTEXT.md C and the Config plan (00-04)

    Cite the two primary sources at the bottom: Claude Code create-plugins docs and plugin-marketplaces docs (URLs in RESEARCH.md §2 sources block).

    AVOID: re-deciding the distribution shape (it's locked in CONTEXT.md A — Option 4 is final), inventing fictional marketplace fields not in the verified schema (RESEARCH.md §2), discussing license-validation backend details (Phase 6), promising a specific orchestration code path without the spike.
  </action>
  <verify>
    1. File exists: `test -f .planning/phases/00-distribution-decision-foundations/ADR-0001-distribution-model.md`
    2. `grep -c "^Status: Accepted" .planning/phases/00-distribution-decision-foundations/ADR-0001-distribution-model.md` returns 1
    3. `grep -E "^## (Context|Decision Drivers|Considered Options|Decision Outcome|Consequences|Confirmation|Open Questions)" .planning/phases/00-distribution-decision-foundations/ADR-0001-distribution-model.md | wc -l` returns ≥7
    4. `grep -c "^- " .planning/phases/00-distribution-decision-foundations/ADR-0001-distribution-model.md` returns ≥15 (decision drivers + consequences + open questions are bulleted)
    5. `wc -l .planning/phases/00-distribution-decision-foundations/ADR-0001-distribution-model.md` returns ≥80 lines
    6. `grep -c "Option [1-4]" .planning/phases/00-distribution-decision-foundations/ADR-0001-distribution-model.md` returns ≥4
  </verify>
  <done>
    ADR-0001 exists, uses MADR 4.0 minimal structure, lists all 4 considered options, marks Option 4 chosen, contains ≥5 open questions for the Phase 2 spike, and references both Claude Code official docs.
  </done>
</task>

</tasks>

<verification>
- File at `.planning/phases/00-distribution-decision-foundations/ADR-0001-distribution-model.md` exists and is ≥80 lines
- Status is Accepted, structure is MADR 4.0 minimal
- All 4 considered options enumerated, Option 4 chosen
- ≥5 open questions documented for Phase 2 spike
- Cross-phase notes mention INST-07, INST-08, Phase 6 SEA binary compatibility, and Phase 6 license prompt
</verification>

<success_criteria>
- ROADMAP.md Phase 0 success criterion #1 ("Distribution model is written down as an accepted ADR") is satisfied by this file
- Phase 2 planning has a clear list of unresolved questions to spike
- No re-deciding of locked CONTEXT.md decisions
</success_criteria>

<output>
After completion, create `.planning/phases/00-distribution-decision-foundations/00-02-SUMMARY.md` listing the ADR sections written and the count of open questions.
</output>
